<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/color_white</item>
    </style>
    
    <style name="tablayout" parent="Widget.Design.TabLayout">
        <item name="tabBackground">@null</item>
        <item name="tabIndicatorHeight">@dimen/dp_2</item>
        <item name="tabIndicatorColor">@color/base_color</item>
        <item name="tabSelectedTextColor">@color/color_333</item>
        <item name="tabTextColor">@color/color_666</item>
        <item name="android:textSize">@dimen/sp_16</item>
    </style>

    <style name="Theme.AppCompat.Translucent" parent="AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>
